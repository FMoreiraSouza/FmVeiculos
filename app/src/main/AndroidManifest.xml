<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FMVeiculos"
        tools:targetApi="31">
        <activity android:name=".view.auth.RegisterActivity" />
        <activity android:name=".view.interests.InterestActivity" />
        <activity android:name=".view.dashboard.DashboardActivity" />
        <activity android:name=".view.catalog.CatalogActivity" />
        <activity android:name=".view.restocking.RestockingActivity" />
        <activity android:name=".view.dealership.CarDetailsActivity" />
        <activity android:name=".view.home.HomeClientActivity" />
        <activity android:name=".view.home.HomeAdminActivity" />
        <activity
            android:name=".view.auth.LoginActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>